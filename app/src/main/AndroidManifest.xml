<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ali.testsaba">

    <uses-permission android:name="android.permission.INTERNET" />


    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="جامعة سبأ  "
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAINN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Aboutuni"
            android:label="@string/title_activity_aboutuni"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Aboutunii"
            android:label="@string/title_activity_aboutunii"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Goals" />
        <activity android:name=".Rules" />
        <activity android:name=".Colleges"
            android:label="الكليات"/>
        <activity
            android:name=".Mediecalde"
            android:label="@string/title_activity_ediecalde"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Dantist"
            android:label="@string/title_activity_dantist"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Computersience"
            android:label="@string/title_activity_computersience"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Engineering"
            android:label="@string/title_activity_engineering"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Laws"
            android:label="@string/title_activity_laws"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Arts"
            android:label="@string/title_activity_arts"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Tarbiah"
            android:label="@string/title_activity_tarbiah"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".callus">
            android:label="تواصل معنا"
            android:theme="@style/AppTheme.NoActionBar"
        </activity>
        <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".reg"
            android:label="@string/title_activity_reg"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".serv"
            android:label="خدمات الجامعه"/>
        <activity
            android:name=".Serv1"
            android:label="@string/title_activity_serv1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Serv2"
            android:label="@string/title_activity_serv2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".kepsi"
            android:label="@string/title_activity_kepsi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Mass" />
        <activity android:name=".Veiw" />
        <activity android:name=".Mom" />
        <activity android:name=".loc" />
        <activity android:name=".Re5" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>